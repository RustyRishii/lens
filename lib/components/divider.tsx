import { StyleSheet, Text, View } from "react-native";
import React from "react";

const Divider = () => {
  return (
    <View style={{ height: 0.5, backgroundColor: "grey", marginVertical: 1 }} />
  );
};

export default Divider;

const styles = StyleSheet.create({});
