import { StyleSheet, Text, View } from "react-native";
import React from "react";

const Modal = (navigation: any) => {
  return (
    <View>
      <Text>MusicSearch</Text>
    </View>
  );
};

export default Modal;

const styles = StyleSheet.create({});
